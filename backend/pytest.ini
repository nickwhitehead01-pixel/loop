[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast in-memory tests, no external dependencies
    integration: require running database and services
    slow: execution time > 2 seconds
